paths:
  /api/v2/ping:
    get:
      summary: Ping Api v2 System
      tags:
        - Ping
      security: [] # No Security
      produces:
        - application/json
      responses:
        200:
          description: Responds with 200 OK if system is up
